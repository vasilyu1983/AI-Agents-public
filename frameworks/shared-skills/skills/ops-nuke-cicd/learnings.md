# ops-nuke-cicd — Raw Learnings

Dated, append-only. Consolidate periodically into `learnings.consolidated.md` via `agents-skills-feedback-loop/scripts/consolidate.py`.

- 2026-07-11: Audit pass (v1.0 → v1.1). Found and fixed a load-bearing factual error repeated in three places (SKILL.md, references/test-platform-modes-and-cli.md, data/sources.json): the skill claimed ".NET 10 SDK enables `TestingPlatformDotnetTestSupport` by default, so repos on .NET 10 are in MTP mode unless explicitly overriding." Verified against learn.microsoft.com/dotnet/core/testing/unit-testing-with-dotnet-test (accessed 2026-07-11): VSTest is explicitly "the default mode for dotnet test" on .NET 10; MTP mode requires an explicit `test.runner` entry in `global.json`, and `TestingPlatformDotnetTestSupport` defaults to `false`. Re-verified NUKE issue #1584 (MTP not supported) is still OPEN via `gh issue view` — that claim was accurate and is now dated 2026-07-11 instead of a stale "May 2026" snapshot. Added a "When Not to Use This Skill" section (non-NUKE pipelines, tool-selection questions, YAML-generator scaffolding, pure test-fixture issues) and an "MTP Migration Decision Framework" (stay on VSTest unless forced, don't partially migrate a solution, re-check #1584 status before long-lived commitments) — the prior version had the antipatterns/checklists but no explicit judgment on whether/when to migrate given the NUKE gap. Confirmed unrelated to this fix: chiseled image / UID 1654 / port 8080 claims (verified against Microsoft .NET 8 container blog), NuGet signature verification default since .NET 8 SDK, and current NUKE release (10.1.0, Dec 2025, repo not archived) — all accurate, no changes needed.
