interface:
  display_name: "SQL Optimization"
  short_description: "Tunes OLTP SQL, query plans, and indexes on PostgreSQL, MySQL, SQL Server."
  default_prompt: "Use $data-sql-optimization to diagnose and tune SQL for OLTP workloads on PostgreSQL, MySQL, and SQL Server: tuning queries, reading plans, indexing, or fixing lock contention."
