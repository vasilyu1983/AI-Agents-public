# Domain Map

> Template stub. The authoritative mapping of **repo → domain**. Every
> repo in `repo-index.md` must appear in exactly one domain here.

## Domains

### &lt;domain&gt;

- **Purpose:** &lt;one line&gt;
- **Repos:** &lt;repo-a&gt;, &lt;repo-b&gt;
- **Owns:** &lt;the data/flows this domain is the system of record for&gt;
- **Upstream:** &lt;domains/repos it depends on&gt;
- **Downstream:** &lt;domains/repos that depend on it&gt;
- **Folder:** `&lt;domain&gt;/`

&lt;Repeat per domain. A repo that genuinely spans domains belongs to the
one that owns its primary data; note the secondary in `Upstream`/
`Downstream`.&gt;

## Unassigned

&lt;Repos discovered but not yet classified. Empty is the goal.&gt;
