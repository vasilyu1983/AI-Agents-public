# &lt;frontend-repo&gt;

> Catalog-page template for a **frontend / app** repo.

- **Domain:** &lt;domain&gt;
- **Kind:** web app | mobile app | desktop app
- **Stack:** &lt;framework / language / build&gt;
- **Status:** active | legacy
- **Confidence:** verified | inferred

## What it is

&lt;User-facing surface and primary flows.&gt;

## Backends consumed

| API / BFF | Owner repo | Contract |
|-----------|-----------|----------|
| &lt;endpoint&gt; | &lt;repo&gt; | &lt;schema ref&gt; |

## Build & runtime

&lt;From project manifests (`package.json`, `project.yml`, `.xcodeproj`,
`Package.swift`) — not README prose. Note excluded generated trees.&gt;

## Provenance

- Profile: `context/graphs/&lt;repo&gt;.json` · Last scan: &lt;date&gt;
