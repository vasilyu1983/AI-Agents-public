interface:
  display_name: "Swarm Orchestration"
  short_description: "Coordinates parallel subagents and multi-agent workflows"
  default_prompt: "Use $agents-swarm-orchestration for Coordinates parallel subagents and multi-agent workflows. Use when splitting work into dependency-aware workers, verifier passes, or isolated research streams."
