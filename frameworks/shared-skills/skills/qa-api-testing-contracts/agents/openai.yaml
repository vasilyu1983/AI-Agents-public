interface:
  display_name: "API Contract QA"
  short_description: "Tests API contracts across REST, GraphQL, gRPC, AsyncAPI, and webhooks."
  default_prompt: "Use $qa-api-testing-contracts for API contract testing across REST, GraphQL, gRPC, AsyncAPI, webhooks, and workflow contracts. Use when you need schema validation, breaking-change detection, and CI quality gates."
